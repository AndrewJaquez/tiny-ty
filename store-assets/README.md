# Store Assets

Included:

- `tiny-yt-promo-440x280.png`: small promo tile for the Chrome Web Store

Still needed manually:

- At least one real screenshot of the extension in use at `1280x800` or `640x400`

Do not use mock screenshots in the store listing. The screenshot should show the actual extension experience in Chrome.
